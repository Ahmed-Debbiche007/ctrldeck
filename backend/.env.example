# Backend Environment Variables
# Copy this file to .env and customize as needed

# Server port (default: 8080)
SERVER_PORT=8080

# Configuration directory for storing buttons, scripts, widgets
# Leave empty for default: ~/.ctrldeck
CONFIG_DIR=

# Static files directory for serving frontend (optional)
# Used when serving the web frontend directly from the backend
STATIC_DIR=
